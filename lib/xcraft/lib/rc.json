{
  "init": {
    "desc": "create main config file in etc",
    "options": {
      "params": {
        "optional": "paths..."
      }
    }
  },
  "bin": {
    "desc": "populate the bin/ directory with base commands",
    "options": {
      "params": {
        "optional": "bins..."
      }
    }
  },
  "defaults": {
    "desc": "create config for all or one installed module(s)",
    "options": {
      "params": {
        "optional": "modules..."
      }
    }
  },
  "configure": {
    "desc": "configure a module",
    "options": {
      "wizard": true,
      "params": {
        "optional": "modules..."
      }
    }
  },
  "modprefix": {
    "desc": "prefix for node_modules directory",
    "options": {
      "params": {
        "required": "location"
      }
    }
  }
}
