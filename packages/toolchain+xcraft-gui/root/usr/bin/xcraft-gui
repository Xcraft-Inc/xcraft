#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../share/toolchain/xcraft-gui/bin/xcraft-gui" "$@"
  ret=$?
else
  node  "$basedir/../share/toolchain/xcraft-gui/bin/xcraft-gui" "$@"
  ret=$?
fi
exit $ret
