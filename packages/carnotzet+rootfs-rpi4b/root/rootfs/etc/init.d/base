#!/bin/sh

sysctl -p >/dev/null

# Configure swap
if [ -f /swapfile ]; then
  swapon /swapfile
fi

# Import fr-CH keyboard map
busybox loadkmap < /usr/share/kmap/fr_CH.kmap

/etc/init.d/network
/etc/init.d/ntp