#!/bin/bash

. ./.node passive

PATH=${node%/*}:$PATH

if [ -d "./node_modules/grunt-jscs" ]; then
  ./node_modules/.bin/grunt --force jscs $@
else
  echo "Install grunt-jscs first"
  echo "$ .npm install grunt-jscs"
fi
