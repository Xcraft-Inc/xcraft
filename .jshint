#!/bin/bash

. ./.node passive

PATH=${node%/*}:$PATH

if [ -d "./node_modules/grunt-contrib-jshint" ]; then
  ./node_modules/.bin/grunt --force jshint $@
else
  echo "Install grunt-contrib-jshint first"
  echo "$ .npm install grunt-contrib-jshint
fi
